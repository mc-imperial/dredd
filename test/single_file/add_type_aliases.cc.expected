#include <cinttypes>
#include <cstddef>

int main() {
  unsigned a;
  uint32_t b;
  int c;
  int32_t d;
  unsigned long e;
  size_t f;
  long g;
  int64_t h;
  uint64_t i;

  a + a;
  b + b;
  c + c;
  d + d;
  e + e;
  f + f;
  g + g;
  h + h;
  i + i;
}
